apiVersion: v1
kind: Service
metadata:
  name: app4
  labels:
    app: app4
    component: web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: app4