# PHP Application Deployment
# Deploys PHP environment information service with Apache web server
apiVersion: apps/v1
kind: Deployment
metadata:
  name: php-app-deployment
  labels:
    my-app: php-backend
    component: php
    tier: backend
spec:
  replicas: 2  # Multiple replicas for availability
  selector:
    matchLabels:
      my-app: php-backend
  template:
    metadata:
      labels:
        my-app: php-backend
        component: php
        tier: backend
    spec:
      containers:
      - name: php-app
        image: app4:latest
        imagePullPolicy: Never  # Use local image only
        ports:
        - containerPort: 80
          name: php-http  # Named port for service reference
        # Resource constraints
        resources:
          requests:
            cpu: "100m"     # Base CPU allocation
            memory: "128Mi" # Base memory allocation
          limits:
            cpu: "500m"     # CPU usage cap
            memory: "256Mi" # Memory usage cap
        # Health monitoring
        livenessProbe:
          httpGet:
            path: /
            port: php-http
          initialDelaySeconds: 10  # Initial warmup time
          periodSeconds: 30        # Check frequency
        readinessProbe:
          httpGet:
            path: /
            port: php-http
          initialDelaySeconds: 5   # Initial warmup time
          periodSeconds: 10        # Check frequency