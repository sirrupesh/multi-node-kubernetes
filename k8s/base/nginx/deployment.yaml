# Nginx Web Server Deployment
# Serves static content with basic HTTP capabilities
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-frontend-deployment
  labels:
    my-app: nginx-frontend
    component: nginx
    tier: frontend
spec:
  replicas: 2  # High availability setup
  selector:
    matchLabels:
      my-app: nginx-frontend
  template:
    metadata:
      labels:
        my-app: nginx-frontend
        component: nginx
        tier: frontend
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
          name: nginx-http  # Named port for service binding
        # Resource allocation
        resources:
          requests:
            cpu: "100m"     # CPU request
            memory: "64Mi"  # Memory request
          limits:
            cpu: "200m"     # CPU limit
            memory: "128Mi" # Memory limit
        # Health checks
        livenessProbe:
          httpGet:
            path: /
            port: nginx-http
          initialDelaySeconds: 10  # Delay before first probe
          periodSeconds: 30        # How often to probe (frequency)
        readinessProbe:
          httpGet:
            path: /
            port: nginx-http
          initialDelaySeconds: 5   # Delay before first probe
          periodSeconds: 10        # How often to probe (frequency)