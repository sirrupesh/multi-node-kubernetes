# Flask Application Kustomization
# Manages Flask app deployment and service resources
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml  # Application pods
  - service.yaml     # Service exposure

labels:
  - includeSelectors: false
    pairs:
      component: web
      tier: backend
      environment: base