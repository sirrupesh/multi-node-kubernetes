# Ingress Kustomization
# Manages cluster ingress configuration
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ingress-resource.yaml  # Ingress routing rules

labels:
  - pairs:
      component: networking
      environment: base