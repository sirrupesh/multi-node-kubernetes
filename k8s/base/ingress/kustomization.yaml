# Routing Config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ingress-resource.yaml  # Routes
  - ingress-class.yaml    # Handler

labels:
  - pairs:
      component: network  # Type
      environment: base  # Tier